# data — never commit raw or processed data.
# data/paper_trades/ is intentionally tracked for cross-device sync of
# the paper-trade log (so phone/laptop/VPS share one settled.csv history).
data/raw/
data/processed/
# model-research: candidate pickles + shadow A/B logs are DATA (gitignored).
# The active-challenger registry json and any tracked ledger markdown under
# data/research/ stay TRACKED (not matched by these patterns).
data/research/candidates/
data/research/shadow/

# env
.env

# python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.venv/
venv/

# notebooks checkpoints
.ipynb_checkpoints/

# OS
.DS_Store
